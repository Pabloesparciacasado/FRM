import itertools

# Espacio muestral
sample_space = [1, 2, 3, 4, 5, 6]

# Eventos: números pares
event = [x for x in sample_space if x % 2 == 0]

# Probabilidad
probability = len(event) / len(sample_space)
print(f"La probabilidad de que salga un número par es: {probability}")
